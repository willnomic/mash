# URL do backend (D-048). Precisa bater com FRONTEND_ORIGIN do backend
# (backend/.env.example) — a origem daqui é o que o backend valida no
# CORS e na checagem mínima de CSRF.
VITE_API_URL="http://localhost:3000"
